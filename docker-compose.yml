version: '3.9'

services:
  pos-integration:
    build: .
    container_name: pos-integration-server
    restart: unless-stopped
    network_mode: host   # 👈 IMPORTANTE
    environment:
      - NODE_ENV=production